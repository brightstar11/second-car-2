<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace+id必须唯一, 建议namespace=dao的全路径-->
<mapper namespace="com.xh.mapper.AdminMapper">
<!-- 与dao的方法名一致 -->
	<!-- 配置 属性与列名的映射 -->
	<!--  
	<resultMap type="com.xh.entity.Admin" id="list">
		<id property="adminid" column="adminid"/>
		<result property="adminName" column="adminName"/>
		<result property="adminPwd" column="adminPwd"/>
		<result property="adminPhone" column="adminPhone"/>
		<result property="adminPermission" column="adminPermission"/>
		<result property="adminMenus" column="adminMenus"/>
		</resultMap>
			判断登录 
		<select id="adminquery" resultMap="list">
	select * from admin where adminName=#{adminName} and adminPwd=#{adminPwd}
	</select>
	判断用户名是否重复 
	<select id="adminnameonly" resultMap="list">
	select adminName from admin
	</select>
		-->

	
	
	<!-- 注册 -->
	<insert id="adminregister" >
	insert into admin(adminname,adminpwd,adminphone,adminpermission) values(#{adminName},#{adminPwd},#{adminPhone},1)
	</insert>
	<!-- 修改个人信息 -->
	<update id="adminupdate" >
	update admin set(adminpwd=#{adminPwd},adminphone=#{adminPhone}) where adminname=#{adminName}
	</update>
</mapper>